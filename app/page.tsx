'use client'

import { useMemo, useState, type Dispatch, type SetStateAction } from 'react'
import {
  Activity,
  BarChart3,
  CheckCircle2,
  Cloud,
  CreditCard,
  Mail,
  Palette,
  PlugZap,
  MessageCircle,
  Target,
  Workflow,
  XCircle,
  ArrowUpRight,
  Bell,
  BriefcaseBusiness,
  CalendarDays,
  ChevronDown,
  CircleHelp,
  Clock3,
  FileText,
  Filter,
  LayoutDashboard,
  LogIn,
  Plus,
  Search,
  Settings2,
  Sparkles,
  Users,
} from 'lucide-react'

type Job = { title: string; team: string; location: string; status: 'Open' | 'Draft'; applicants: number; updated: string }
type UploadItem = { id: number; name: string; kind: 'JD' | 'Resume'; size: string; status: 'Uploaded' | 'Processing'; candidate?: string }
type CandidateProfile = { id?: string; fullName: string; email?: string; summary?: string; skills: string[]; sourceFile: string; extractionProvider: string }
type MatchResult = { candidate: CandidateProfile; score: number; matched: string[]; missing: string[] }

const matchingRoles = [
  { title: 'Senior Product Designer', skills: ['Figma', 'User research', 'Product strategy', 'Leadership', 'Design systems'] },
  { title: 'Staff Software Engineer', skills: ['TypeScript', 'React', 'Node.js', 'Python', 'Leadership'] },
  { title: 'Growth Marketing Lead', skills: ['Product strategy', 'SQL', 'Leadership', 'Analytics', 'Content strategy'] },
]

const initialUploads: UploadItem[] = [
  { id: 1, name: 'senior-product-designer.pdf', kind: 'JD', size: '248 KB', status: 'Uploaded' },
  { id: 2, name: 'maya-chen-resume.pdf', kind: 'Resume', size: '1.2 MB', status: 'Uploaded', candidate: 'Maya Chen' },
  { id: 3, name: 'daniel-okafor-resume.docx', kind: 'Resume', size: '864 KB', status: 'Uploaded', candidate: 'Daniel Okafor' },
]

const initialJobs: Job[] = [
  { title: 'Senior Product Designer', team: 'Product', location: 'Remote · US', status: 'Open', applicants: 48, updated: '2 hours ago' },
  { title: 'Staff Software Engineer', team: 'Engineering', location: 'New York · Hybrid', status: 'Open', applicants: 72, updated: 'Yesterday' },
  { title: 'Growth Marketing Lead', team: 'Marketing', location: 'London · Hybrid', status: 'Open', applicants: 31, updated: 'Yesterday' },
  { title: 'Product Manager', team: 'Product', location: 'Remote · Europe', status: 'Draft', applicants: 0, updated: '3 days ago' },
]

const candidates = [
  { name: 'Maya Chen', role: 'Senior Product Designer', stage: 'Interview', score: '94%', initials: 'MC', color: 'bg-sky-100 text-sky-700', time: 'Today, 11:30 AM' },
  { name: 'Daniel Okafor', role: 'Staff Software Engineer', stage: 'Review', score: '88%', initials: 'DO', color: 'bg-violet-100 text-violet-700', time: 'Today, 2:00 PM' },
  { name: 'Sofia Alvarez', role: 'Growth Marketing Lead', stage: 'Offer', score: '91%', initials: 'SA', color: 'bg-amber-100 text-amber-700', time: 'Tomorrow, 9:00 AM' },
  { name: 'Liam Patel', role: 'Product Manager', stage: 'Sourcing', score: '82%', initials: 'LP', color: 'bg-emerald-100 text-emerald-700', time: 'Tomorrow, 3:30 PM' },
]

const activities = [
  { icon: CalendarDays, title: 'Interview scheduled', detail: 'Maya Chen · Senior Product Designer', time: '8 min ago', tone: 'bg-sky-50 text-sky-600' },
  { icon: FileText, title: 'Resume added to pipeline', detail: 'Daniel Okafor · Staff Engineer', time: '34 min ago', tone: 'bg-violet-50 text-violet-600' },
  { icon: Sparkles, title: 'AI shortlist ready', detail: '12 candidates matched for Product Manager', time: '1 hr ago', tone: 'bg-amber-50 text-amber-600' },
]

export default function Page() {
  const [signedIn, setSignedIn] = useState(false)
  const [organization, setOrganization] = useState('')
  const [workspaceReady, setWorkspaceReady] = useState(false)
  const [authMode, setAuthMode] = useState<'signin' | 'signup'>('signin')
  const [activeView, setActiveView] = useState('Overview')
  const [query, setQuery] = useState('')
  const [jobs, setJobs] = useState(initialJobs)
  const [notice, setNotice] = useState('')
  const [uploads, setUploads] = useState<UploadItem[]>(initialUploads)
  const [profiles, setProfiles] = useState<CandidateProfile[]>([])
  const [pipelineStages, setPipelineStages] = useState<Record<string, string>>({ 'Maya Chen': 'Interview', 'Daniel Okafor': 'Review', 'Sofia Alvarez': 'Offer', 'Liam Patel': 'Sourcing' })

  function announce(message: string) {
    setNotice(message)
    window.setTimeout(() => setNotice(''), 2600)
  }

  if (!signedIn) {
    return <AuthScreen mode={authMode} onModeChange={setAuthMode} onContinue={() => setSignedIn(true)} />
  }

  if (!workspaceReady) {
    return <OrganizationSetup onComplete={(name) => { setOrganization(name); setWorkspaceReady(true) }} onSignOut={() => setSignedIn(false)} />
  }

  const filteredCandidates = candidates.filter((candidate) => `${candidate.name} ${candidate.role}`.toLowerCase().includes(query.toLowerCase()))
  const createJob = () => {
    setJobs((current) => [{ title: 'New role', team: 'Your team', location: 'Remote', status: 'Draft', applicants: 0, updated: 'Just now' }, ...current])
    setActiveView('Jobs')
    announce('Draft job created')
  }

  return (
    <main className="min-h-screen bg-background text-foreground">
      {notice && <div role="status" className="fixed bottom-5 left-1/2 z-50 -translate-x-1/2 rounded-full bg-primary px-5 py-3 text-sm font-medium text-primary-foreground shadow-lg">{notice}</div>}
      <header className="sticky top-0 z-40 border-b border-border/70 bg-background/90 backdrop-blur-xl">
        <div className="mx-auto flex max-w-[1440px] items-center justify-between gap-6 px-5 py-4 lg:px-10">
          <div className="flex items-center gap-3"><div className="flex size-10 items-center justify-center rounded-xl bg-primary text-primary-foreground shadow-sm"><Sparkles className="size-5" /></div><div><p className="text-[17px] font-semibold tracking-tight">talent<span className="text-primary">flow</span></p><p className="text-[11px] font-medium uppercase tracking-[0.18em] text-muted-foreground">Recruiting OS</p></div></div>
          <nav className="hidden items-center gap-1 rounded-full border border-border bg-card p-1 lg:flex" aria-label="Primary navigation">{['Overview', 'Candidates', 'Jobs', 'Analytics'].map((item) => <button key={item} onClick={() => setActiveView(item)} className={`rounded-full px-4 py-2 text-sm font-medium transition ${activeView === item ? 'bg-primary text-primary-foreground shadow-sm' : 'text-muted-foreground hover:bg-muted hover:text-foreground'}`}>{item}</button>)}</nav>
          <div className="flex items-center gap-2"><button onClick={() => announce('No new notifications')} aria-label="Notifications" className="hidden size-10 items-center justify-center rounded-full border border-border bg-card text-muted-foreground sm:flex"><Bell className="size-[18px]" /></button><button onClick={() => announce('Help center opened')} aria-label="Help" className="hidden size-10 items-center justify-center rounded-full border border-border bg-card text-muted-foreground sm:flex"><CircleHelp className="size-[18px]" /></button><button onClick={() => announce(`${organization} workspace`)} className="flex items-center gap-2 rounded-full border border-border bg-card py-1.5 pl-1.5 pr-3 text-left"><span className="flex size-8 items-center justify-center rounded-full bg-foreground text-xs font-semibold text-background">AR</span><span className="hidden text-sm font-medium sm:block">Alex Rivera</span><ChevronDown className="hidden size-4 text-muted-foreground sm:block" /></button></div>
        </div>
      </header>
      <div className="mx-auto grid max-w-[1440px] gap-8 px-5 py-8 lg:grid-cols-[220px_1fr] lg:px-10 lg:py-10">
        <aside className="hidden lg:block"><div className="sticky top-28 flex flex-col gap-7"><div><p className="mb-3 px-3 text-[11px] font-semibold uppercase tracking-[0.16em] text-muted-foreground">{organization}</p><div className="flex flex-col gap-1">{[{ icon: LayoutDashboard, label: 'Overview' }, { icon: Users, label: 'Candidates' }, { icon: BriefcaseBusiness, label: 'Jobs' }, { icon: CalendarDays, label: 'Interviews' }, { icon: FileText, label: 'Intake' }, { icon: Sparkles, label: 'Profiles' }, { icon: Target, label: 'Matching' }, { icon: Workflow, label: 'Pipeline' }, { icon: BarChart3, label: 'Analytics' }].map(({ icon: Icon, label }) => <button key={label} onClick={() => setActiveView(label)} className={`flex items-center gap-3 rounded-xl px-3 py-2.5 text-sm font-medium transition ${activeView === label ? 'bg-card text-primary shadow-sm ring-1 ring-border' : 'text-muted-foreground hover:bg-card hover:text-foreground'}`}><Icon className="size-[18px]" />{label}</button>)}</div></div><div><p className="mb-3 px-3 text-[11px] font-semibold uppercase tracking-[0.16em] text-muted-foreground">Manage</p><div className="flex flex-col gap-1"><button onClick={() => setActiveView('Integrations')} className="flex items-center gap-3 rounded-xl px-3 py-2.5 text-sm font-medium text-muted-foreground hover:bg-card hover:text-foreground"><PlugZap className="size-[18px]" />Integrations</button><button onClick={() => announce('Reports are coming soon')} className="flex items-center gap-3 rounded-xl px-3 py-2.5 text-sm font-medium text-muted-foreground hover:bg-card hover:text-foreground"><Activity className="size-[18px]" />Reports</button><button onClick={() => announce('Settings opened')} className="flex items-center gap-3 rounded-xl px-3 py-2.5 text-sm font-medium text-muted-foreground hover:bg-card hover:text-foreground"><Settings2 className="size-[18px]" />Settings</button></div></div><div className="rounded-2xl bg-primary p-4 text-primary-foreground shadow-lg shadow-primary/10"><p className="mb-2 text-sm font-semibold">Phase 1 complete</p><p className="mb-4 text-xs leading-5 text-primary-foreground/75">Auth, workspace, dashboard, and jobs are ready.</p><button onClick={() => setActiveView('Jobs')} className="flex w-full items-center justify-between rounded-lg bg-primary-foreground/15 px-3 py-2 text-xs font-semibold hover:bg-primary-foreground/25">Manage jobs <ArrowUpRight className="size-4" /></button></div></div></aside>
        <section className="min-w-0">{activeView === 'Jobs' ? <JobsView jobs={jobs} onCreate={createJob} onNotice={announce} /> : activeView === 'Intake' ? <IntakeView uploads={uploads} setUploads={setUploads} onNotice={announce} /> : activeView === 'Profiles' ? <ProfilesView profiles={profiles} onNotice={announce} /> : activeView === 'Matching' ? <MatchingView profiles={profiles} onNotice={announce} /> : activeView === 'Pipeline' ? <PipelineView pipelineStages={pipelineStages} setPipelineStages={setPipelineStages} onNotice={announce} /> : activeView === 'Analytics' ? <AnalyticsView onNotice={announce} /> : activeView === 'Integrations' ? <IntegrationsView onNotice={announce} /> : <DashboardView activeView={activeView} query={query} setQuery={setQuery} filteredCandidates={filteredCandidates} onCreate={createJob} onNavigate={setActiveView} onNotice={announce} />}</section>
      </div>
    </main>
  )
}

function AuthScreen({ mode, onModeChange, onContinue }: { mode: 'signin' | 'signup'; onModeChange: (mode: 'signin' | 'signup') => void; onContinue: () => void }) {
  return <main className="flex min-h-screen items-center justify-center bg-background px-5 py-10"><div className="w-full max-w-md"><div className="mb-8 flex items-center justify-center gap-3"><div className="flex size-11 items-center justify-center rounded-xl bg-primary text-primary-foreground"><Sparkles className="size-5" /></div><span className="text-xl font-semibold tracking-tight">talent<span className="text-primary">flow</span></span></div><div className="rounded-3xl border border-border bg-card p-7 shadow-[0_20px_60px_-35px_rgba(25,45,75,0.4)] sm:p-9"><div className="mb-7"><p className="mb-2 text-sm font-semibold text-primary">Phase 1 · Authentication</p><h1 className="text-3xl font-semibold tracking-tight">{mode === 'signin' ? 'Welcome back' : 'Create your workspace'}</h1><p className="mt-2 text-sm leading-6 text-muted-foreground">{mode === 'signin' ? 'Sign in to continue to your hiring workspace.' : 'Start organizing your hiring team in one calm place.'}</p></div><div className="flex flex-col gap-4"><label className="flex flex-col gap-2 text-sm font-medium">Work email<input type="email" defaultValue="alex@company.com" className="rounded-xl border border-input bg-background px-4 py-3 outline-none ring-primary focus:ring-2" /></label><label className="flex flex-col gap-2 text-sm font-medium">Password<input type="password" defaultValue="password123" className="rounded-xl border border-input bg-background px-4 py-3 outline-none ring-primary focus:ring-2" /></label><button onClick={onContinue} className="mt-2 flex items-center justify-center gap-2 rounded-xl bg-primary px-4 py-3 font-semibold text-primary-foreground hover:bg-primary/90"><LogIn className="size-4" />{mode === 'signin' ? 'Sign in' : 'Create account'}</button></div><p className="mt-6 text-center text-sm text-muted-foreground">{mode === 'signin' ? 'New to talentflow?' : 'Already have an account?'} <button onClick={() => onModeChange(mode === 'signin' ? 'signup' : 'signin')} className="font-semibold text-primary hover:underline">{mode === 'signin' ? 'Create an account' : 'Sign in'}</button></p></div></div></main>
}

function OrganizationSetup({ onComplete, onSignOut }: { onComplete: (name: string) => void; onSignOut: () => void }) {
  const [name, setName] = useState('Northstar Labs')
  return <main className="flex min-h-screen items-center justify-center bg-background px-5 py-10"><div className="w-full max-w-lg rounded-3xl border border-border bg-card p-8 shadow-[0_20px_60px_-35px_rgba(25,45,75,0.4)]"><div className="mb-8 flex items-center justify-between"><div className="flex size-11 items-center justify-center rounded-xl bg-primary text-primary-foreground"><BriefcaseBusiness className="size-5" /></div><button onClick={onSignOut} className="text-sm text-muted-foreground hover:text-foreground">Sign out</button></div><p className="mb-2 text-sm font-semibold text-primary">Phase 1 · Organization</p><h1 className="text-3xl font-semibold tracking-tight">Set up your hiring workspace</h1><p className="mt-2 text-sm leading-6 text-muted-foreground">Every job, candidate, and teammate will live inside this organization.</p><label className="mt-8 flex flex-col gap-2 text-sm font-medium">Organization name<input value={name} onChange={(e) => setName(e.target.value)} className="rounded-xl border border-input bg-background px-4 py-3 outline-none ring-primary focus:ring-2" /></label><button disabled={!name.trim()} onClick={() => onComplete(name.trim())} className="mt-5 flex w-full items-center justify-center gap-2 rounded-xl bg-primary px-4 py-3 font-semibold text-primary-foreground hover:bg-primary/90 disabled:cursor-not-allowed disabled:opacity-50">Continue to dashboard <ArrowUpRight className="size-4" /></button></div></main>
}

function DashboardView({ activeView, query, setQuery, filteredCandidates, onCreate, onNavigate, onNotice }: any) {
  return <><div className="mb-8 flex flex-col justify-between gap-5 md:flex-row md:items-end"><div><div className="mb-3 inline-flex items-center gap-2 rounded-full border border-border bg-card px-3 py-1.5 text-xs font-semibold text-primary"><span className="size-1.5 rounded-full bg-emerald-500" />Phase 1 workspace</div><h1 className="text-balance text-3xl font-semibold tracking-tight md:text-4xl">Good morning, Alex.</h1><p className="mt-2 text-base text-muted-foreground">Here&apos;s what&apos;s moving across your hiring workspace.</p></div><button onClick={onCreate} className="inline-flex items-center justify-center gap-2 rounded-xl bg-primary px-5 py-3 text-sm font-semibold text-primary-foreground shadow-md shadow-primary/15 hover:bg-primary/90"><Plus className="size-4" /> Create a job</button></div><div className="grid gap-4 sm:grid-cols-2 xl:grid-cols-4">{[['Active candidates', '248', '+18.4%', Users, 'bg-sky-50 text-sky-600'], ['Open positions', '16', '+3 this month', BriefcaseBusiness, 'bg-violet-50 text-violet-600'], ['Interviews this week', '32', '8 today', CalendarDays, 'bg-amber-50 text-amber-600'], ['Time to hire', '18.6d', '-12.8%', Clock3, 'bg-emerald-50 text-emerald-600']].map(([label, value, change, Icon, tone]: any) => <div key={label} className="rounded-2xl border border-border bg-card p-5 shadow-[0_10px_30px_-24px_rgba(25,45,75,0.35)]"><div className="mb-5 flex items-center justify-between"><span className={`flex size-10 items-center justify-center rounded-xl ${tone}`}><Icon className="size-5" /></span><span className="rounded-full bg-muted px-2.5 py-1 text-[11px] font-semibold text-muted-foreground">{change}</span></div><p className="text-sm text-muted-foreground">{label}</p><p className="mt-1 text-2xl font-semibold tracking-tight">{value}</p></div>)}</div><div className="mt-8 grid gap-6 xl:grid-cols-[1.5fr_1fr]"><div className="min-w-0 rounded-2xl border border-border bg-card shadow-[0_10px_30px_-24px_rgba(25,45,75,0.35)]"><div className="flex flex-col gap-4 border-b border-border p-5 sm:flex-row sm:items-center sm:justify-between"><div><h2 className="font-semibold tracking-tight">Candidate pipeline</h2><p className="mt-1 text-sm text-muted-foreground">{activeView === 'Candidates' ? 'All candidates matched to your search' : 'Your most recent candidate activity'}</p></div><div className="flex items-center gap-2"><label className="flex min-w-0 items-center gap-2 rounded-lg border border-border bg-background px-3 py-2 text-sm text-muted-foreground"><Search className="size-4 shrink-0" /><input value={query} onChange={(e) => setQuery(e.target.value)} aria-label="Search candidates" placeholder="Search candidates" className="min-w-0 bg-transparent outline-none placeholder:text-muted-foreground/70" /></label><button onClick={() => onNotice('Pipeline filters opened')} aria-label="Filter pipeline" className="flex size-9 items-center justify-center rounded-lg border border-border text-muted-foreground hover:text-foreground"><Filter className="size-4" /></button></div></div><div className="overflow-x-auto"><table className="w-full min-w-[640px] text-left"><thead className="bg-muted/45 text-xs font-semibold uppercase tracking-wider text-muted-foreground"><tr><th className="px-5 py-3">Candidate</th><th className="px-5 py-3">Stage</th><th className="px-5 py-3">AI match</th><th className="px-5 py-3">Next step</th></tr></thead><tbody className="divide-y divide-border">{filteredCandidates.map((candidate: any) => <tr key={candidate.name} className="hover:bg-muted/30"><td className="px-5 py-4"><div className="flex items-center gap-3"><span className={`flex size-9 items-center justify-center rounded-full text-xs font-semibold ${candidate.color}`}>{candidate.initials}</span><div><p className="text-sm font-semibold">{candidate.name}</p><p className="mt-0.5 text-xs text-muted-foreground">{candidate.role}</p></div></div></td><td className="px-5 py-4"><span className="rounded-full bg-muted px-2.5 py-1 text-xs font-medium">{candidate.stage}</span></td><td className="px-5 py-4 text-sm font-semibold text-primary">{candidate.score}</td><td className="px-5 py-4 text-xs text-muted-foreground">{candidate.time}</td></tr>)}</tbody></table></div><button onClick={() => onNavigate('Candidates')} className="flex w-full items-center justify-center gap-1 border-t border-border p-4 text-sm font-semibold text-primary hover:bg-muted/40">View all candidates <ArrowUpRight className="size-4" /></button></div><div className="rounded-2xl border border-border bg-card p-5 shadow-[0_10px_30px_-24px_rgba(25,45,75,0.35)]"><div className="mb-5 flex items-start justify-between"><div><h2 className="font-semibold tracking-tight">Recent activity</h2><p className="mt-1 text-sm text-muted-foreground">Updates from your team</p></div><button onClick={() => onNotice('Activity feed opened')} className="text-xs font-semibold text-primary hover:underline">View all</button></div><div className="flex flex-col gap-5">{activities.map(({ icon: Icon, title, detail, time, tone }) => <div key={title} className="flex gap-3"><span className={`flex size-9 shrink-0 items-center justify-center rounded-xl ${tone}`}><Icon className="size-4" /></span><div><p className="text-sm font-medium">{title}</p><p className="mt-1 text-xs leading-5 text-muted-foreground">{detail}</p><p className="mt-1.5 text-[11px] text-muted-foreground/70">{time}</p></div></div>)}</div><div className="mt-6 rounded-xl bg-muted/60 p-4"><div className="flex items-center gap-2"><span className="flex size-7 items-center justify-center rounded-lg bg-card text-primary"><Sparkles className="size-3.5" /></span><p className="text-xs font-semibold">Next phase</p></div><p className="mt-2 text-xs leading-5 text-muted-foreground">Upload a job description and resumes to start building your candidate database.</p><button onClick={() => onNotice('Phase 2 is next')} className="mt-3 flex items-center gap-1 text-xs font-semibold text-primary">Plan Phase 2 <ArrowUpRight className="size-3.5" /></button></div></div></div></>
}

function JobsView({ jobs, onCreate, onNotice }: { jobs: Job[]; onCreate: () => void; onNotice: (message: string) => void }) {
  return <><div className="mb-8 flex flex-col justify-between gap-5 md:flex-row md:items-end"><div><div className="mb-3 inline-flex items-center gap-2 rounded-full border border-border bg-card px-3 py-1.5 text-xs font-semibold text-primary"><BriefcaseBusiness className="size-3.5" />Phase 1 · Jobs</div><h1 className="text-3xl font-semibold tracking-tight md:text-4xl">Your open roles.</h1><p className="mt-2 text-base text-muted-foreground">Create, organize, and move every role forward.</p></div><button onClick={onCreate} className="inline-flex items-center justify-center gap-2 rounded-xl bg-primary px-5 py-3 text-sm font-semibold text-primary-foreground shadow-md shadow-primary/15 hover:bg-primary/90"><Plus className="size-4" /> Create a job</button></div><div className="grid gap-4 sm:grid-cols-3"><div className="rounded-2xl border border-border bg-card p-5"><p className="text-sm text-muted-foreground">Total roles</p><p className="mt-1 text-2xl font-semibold">{jobs.length}</p></div><div className="rounded-2xl border border-border bg-card p-5"><p className="text-sm text-muted-foreground">Open roles</p><p className="mt-1 text-2xl font-semibold">{jobs.filter((j) => j.status === 'Open').length}</p></div><div className="rounded-2xl border border-border bg-card p-5"><p className="text-sm text-muted-foreground">Applicants</p><p className="mt-1 text-2xl font-semibold">{jobs.reduce((sum, job) => sum + job.applicants, 0)}</p></div></div><div className="mt-8 overflow-hidden rounded-2xl border border-border bg-card"><div className="flex items-center justify-between border-b border-border p-5"><div><h2 className="font-semibold">All jobs</h2><p className="mt-1 text-sm text-muted-foreground">Workspace-scoped role management</p></div><button onClick={() => onNotice('Job filters opened')} aria-label="Filter jobs" className="flex size-9 items-center justify-center rounded-lg border border-border text-muted-foreground hover:text-foreground"><Filter className="size-4" /></button></div><div className="overflow-x-auto"><table className="w-full min-w-[680px] text-left"><thead className="bg-muted/45 text-xs font-semibold uppercase tracking-wider text-muted-foreground"><tr><th className="px-5 py-3">Role</th><th className="px-5 py-3">Location</th><th className="px-5 py-3">Status</th><th className="px-5 py-3">Applicants</th><th className="px-5 py-3">Updated</th><th className="px-5 py-3"><span className="sr-only">Actions</span></th></tr></thead><tbody className="divide-y divide-border">{jobs.map((job) => <tr key={`${job.title}-${job.updated}`} className="hover:bg-muted/30"><td className="px-5 py-4"><p className="text-sm font-semibold">{job.title}</p><p className="mt-1 text-xs text-muted-foreground">{job.team}</p></td><td className="px-5 py-4 text-sm text-muted-foreground">{job.location}</td><td className="px-5 py-4"><span className={`rounded-full px-2.5 py-1 text-xs font-medium ${job.status === 'Open' ? 'bg-emerald-50 text-emerald-700' : 'bg-muted text-muted-foreground'}`}>{job.status}</span></td><td className="px-5 py-4 text-sm font-semibold">{job.applicants}</td><td className="px-5 py-4 text-xs text-muted-foreground">{job.updated}</td><td className="px-5 py-4"><button onClick={() => onNotice(`${job.title} selected`)} className="rounded-lg border border-border px-3 py-1.5 text-xs font-semibold hover:bg-muted">Open</button></td></tr>)}</tbody></table></div></div></>
}

function MatchingView({ profiles, onNotice }: { profiles: CandidateProfile[]; onNotice: (message: string) => void }) {
  const [roleIndex, setRoleIndex] = useState(0)
  const role = matchingRoles[roleIndex]
  const results: MatchResult[] = profiles.map((candidate) => {
    const normalized = candidate.skills.map((skill) => skill.toLowerCase())
    const matched = role.skills.filter((skill) => normalized.includes(skill.toLowerCase()))
    return { candidate, score: Math.round((matched.length / role.skills.length) * 100), matched, missing: role.skills.filter((skill) => !normalized.includes(skill.toLowerCase())) }
  }).sort((a, b) => b.score - a.score)
  return <div><div className="mb-8 flex flex-col justify-between gap-5 md:flex-row md:items-end"><div><div className="mb-3 inline-flex items-center gap-2 rounded-full border border-border bg-card px-3 py-1.5 text-xs font-semibold text-primary"><Target className="size-3.5" />Phase 4 · Matching</div><h1 className="text-3xl font-semibold tracking-tight md:text-4xl">Find the strongest match.</h1><p className="mt-2 max-w-2xl text-base text-muted-foreground">Compare role requirements against validated candidate profiles with an explainable skills-first score.</p></div><button onClick={() => onNotice('Match results refreshed')} className="rounded-xl bg-primary px-5 py-3 text-sm font-semibold text-primary-foreground">Refresh matches</button></div><div className="rounded-2xl border border-border bg-card p-5"><div className="flex flex-col gap-4 md:flex-row md:items-center md:justify-between"><div><p className="text-xs font-semibold uppercase tracking-[0.16em] text-primary">Compare candidates for</p><h2 className="mt-1 text-xl font-semibold">{role.title}</h2><p className="mt-1 text-sm text-muted-foreground">{role.skills.length} required skills · score weighted by skill coverage</p></div><select value={roleIndex} onChange={(event) => setRoleIndex(Number(event.target.value))} className="rounded-xl border border-input bg-background px-4 py-3 text-sm font-medium outline-none focus:ring-2 focus:ring-primary">{matchingRoles.map((item, index) => <option value={index} key={item.title}>{item.title}</option>)}</select></div><div className="mt-5 flex flex-wrap gap-2">{role.skills.map((skill) => <span key={skill} className="rounded-full bg-muted px-3 py-1.5 text-xs font-medium text-muted-foreground">{skill}</span>)}</div></div><div className="mt-6 flex items-center justify-between"><div><h2 className="font-semibold">Candidate matches</h2><p className="mt-1 text-sm text-muted-foreground">{results.length} profiles evaluated for this role</p></div><span className="rounded-full bg-muted px-3 py-1.5 text-xs font-semibold text-muted-foreground">Explainable scoring</span></div><div className="mt-4 flex flex-col gap-4">{results.length === 0 ? <div className="rounded-2xl border border-dashed border-border bg-card p-12 text-center"><Target className="mx-auto mb-3 size-7 text-primary" /><h2 className="font-semibold">No profiles to match yet</h2><p className="mx-auto mt-2 max-w-md text-sm leading-6 text-muted-foreground">Process resumes in Intake first, then return here to compare them against a role.</p></div> : results.map((result) => <article key={result.candidate.id || result.candidate.sourceFile} className="rounded-2xl border border-border bg-card p-5"><div className="flex flex-col gap-4 md:flex-row md:items-start md:justify-between"><div><h3 className="text-lg font-semibold">{result.candidate.fullName}</h3><p className="mt-1 text-sm text-muted-foreground">{result.candidate.email || result.candidate.sourceFile}</p></div><div className="flex items-center gap-3"><div className="text-right"><p className="text-2xl font-semibold tracking-tight text-primary">{result.score}%</p><p className="text-xs text-muted-foreground">skill match</p></div><span className={`rounded-full px-3 py-1.5 text-xs font-semibold ${result.score >= 70 ? 'bg-emerald-50 text-emerald-700' : result.score >= 40 ? 'bg-amber-50 text-amber-700' : 'bg-muted text-muted-foreground'}`}>{result.score >= 70 ? 'Shortlist' : result.score >= 40 ? 'Review' : 'Low match'}</span></div></div><div className="mt-5 grid gap-4 md:grid-cols-2"><div><p className="mb-2 flex items-center gap-2 text-xs font-semibold uppercase tracking-[0.14em] text-emerald-700"><CheckCircle2 className="size-4" />Matched skills</p><div className="flex flex-wrap gap-2">{result.matched.length ? result.matched.map((skill) => <span key={skill} className="rounded-full bg-emerald-50 px-2.5 py-1 text-xs font-medium text-emerald-700">{skill}</span>) : <span className="text-sm text-muted-foreground">No required skills detected</span>}</div></div><div><p className="mb-2 flex items-center gap-2 text-xs font-semibold uppercase tracking-[0.14em] text-rose-700"><XCircle className="size-4" />Missing skills</p><div className="flex flex-wrap gap-2">{result.missing.length ? result.missing.map((skill) => <span key={skill} className="rounded-full bg-rose-50 px-2.5 py-1 text-xs font-medium text-rose-700">{skill}</span>) : <span className="text-sm text-emerald-700">All required skills covered</span>}</div></div></div></article>)}</div></div>
}

function ProfilesView({ profiles, onNotice }: { profiles: CandidateProfile[]; onNotice: (message: string) => void }) {
  return <div><div className="mb-8 flex flex-col justify-between gap-5 md:flex-row md:items-end"><div><div className="mb-3 inline-flex items-center gap-2 rounded-full border border-border bg-card px-3 py-1.5 text-xs font-semibold text-primary"><Sparkles className="size-3.5" />Phase 3 · Candidate profiles</div><h1 className="text-3xl font-semibold tracking-tight md:text-4xl">Structured profiles, ready to match.</h1><p className="mt-2 max-w-2xl text-base text-muted-foreground">Extracted resume text is validated with Zod and stored as candidate profile documents in MongoDB Atlas.</p></div><button onClick={() => onNotice('Open Intake to process another resume')} className="rounded-xl bg-primary px-5 py-3 text-sm font-semibold text-primary-foreground">Process a resume</button></div><div className="grid gap-4 sm:grid-cols-3"><div className="rounded-2xl border border-border bg-card p-5"><p className="text-sm text-muted-foreground">Profiles ready</p><p className="mt-1 text-2xl font-semibold">{profiles.length}</p></div><div className="rounded-2xl border border-border bg-card p-5"><p className="text-sm text-muted-foreground">Validation</p><p className="mt-1 text-2xl font-semibold text-emerald-600">Zod</p></div><div className="rounded-2xl border border-border bg-card p-5"><p className="text-sm text-muted-foreground">AI provider</p><p className="mt-1 text-2xl font-semibold">Gemini</p></div></div><div className="mt-6 flex flex-col gap-4">{profiles.length === 0 ? <div className="rounded-2xl border border-dashed border-border bg-card p-12 text-center"><Sparkles className="mx-auto mb-3 size-7 text-primary" /><h2 className="font-semibold">No extracted profiles yet</h2><p className="mx-auto mt-2 max-w-md text-sm leading-6 text-muted-foreground">Upload a resume from Intake to extract text, validate the profile shape, and save it to MongoDB Atlas.</p></div> : profiles.map((profile) => <article key={profile.id || profile.sourceFile} className="rounded-2xl border border-border bg-card p-5"><div className="flex flex-col gap-4 md:flex-row md:items-start md:justify-between"><div><h2 className="text-lg font-semibold">{profile.fullName}</h2><p className="mt-1 text-sm text-muted-foreground">{profile.email || 'Email not detected'} · {profile.sourceFile}</p></div><span className="rounded-full bg-emerald-50 px-3 py-1.5 text-xs font-semibold text-emerald-700">Validated · {profile.extractionProvider}</span></div><p className="mt-4 max-w-3xl text-sm leading-6 text-muted-foreground">{profile.summary || 'No summary extracted.'}</p><div className="mt-4 flex flex-wrap gap-2">{profile.skills.map((skill) => <span key={skill} className="rounded-full bg-muted px-2.5 py-1 text-xs font-medium text-muted-foreground">{skill}</span>)}</div></article>)}</div></div>
}

function PipelineView({ pipelineStages, setPipelineStages, onNotice }: { pipelineStages: Record<string, string>; setPipelineStages: Dispatch<SetStateAction<Record<string, string>>>; onNotice: (message: string) => void }) {
  const stages = ['Sourcing', 'Review', 'Interview', 'Offer']
  const moveCandidate = (name: string, current: string) => { const next = stages[(stages.indexOf(current) + 1) % stages.length]; setPipelineStages((items) => ({ ...items, [name]: next })); onNotice(`${name} moved to ${next}`) }
  return <div><div className="mb-8 flex flex-col justify-between gap-5 md:flex-row md:items-end"><div><div className="mb-3 inline-flex items-center gap-2 rounded-full border border-border bg-card px-3 py-1.5 text-xs font-semibold text-primary"><Workflow className="size-3.5" />Phase 5 · Pipeline</div><h1 className="text-3xl font-semibold tracking-tight md:text-4xl">Move every candidate forward.</h1><p className="mt-2 max-w-2xl text-base text-muted-foreground">A focused pipeline for your team, with workflow actions and stage-level visibility.</p></div><button onClick={() => onNotice('Workflow builder opened')} className="rounded-xl bg-primary px-5 py-3 text-sm font-semibold text-primary-foreground">Open workflow builder</button></div><div className="mb-6 grid gap-4 sm:grid-cols-4">{stages.map((stage) => <div key={stage} className="rounded-2xl border border-border bg-card p-4"><p className="text-sm text-muted-foreground">{stage}</p><p className="mt-1 text-2xl font-semibold">{Object.values(pipelineStages).filter((value) => value === stage).length}</p></div>)}</div><div className="grid gap-4 xl:grid-cols-4">{stages.map((stage) => <div key={stage} className="min-h-64 rounded-2xl border border-border bg-muted/30 p-3"><div className="mb-3 flex items-center justify-between px-1"><h2 className="text-sm font-semibold">{stage}</h2><span className="text-xs text-muted-foreground">{Object.values(pipelineStages).filter((value) => value === stage).length}</span></div><div className="flex flex-col gap-3">{Object.entries(pipelineStages).filter(([, value]) => value === stage).map(([name]) => <div key={name} className="rounded-xl border border-border bg-card p-4 shadow-sm"><div className="flex items-start justify-between gap-3"><div><p className="text-sm font-semibold">{name}</p><p className="mt-1 text-xs text-muted-foreground">{name === 'Maya Chen' ? 'Senior Product Designer' : name === 'Daniel Okafor' ? 'Staff Software Engineer' : name === 'Sofia Alvarez' ? 'Growth Marketing Lead' : 'Product Manager'}</p></div><span className="flex size-8 items-center justify-center rounded-full bg-primary/10 text-xs font-semibold text-primary">{name.split(' ').map((part) => part[0]).join('')}</span></div><button onClick={() => moveCandidate(name, stage)} className="mt-4 w-full rounded-lg border border-border px-3 py-2 text-xs font-semibold text-muted-foreground hover:text-foreground">Move to next stage</button></div>)}</div></div>)}</div><div className="mt-6 rounded-2xl border border-border bg-card p-5"><div className="flex items-center gap-3"><Workflow className="size-5 text-primary" /><div><h2 className="font-semibold">Workflow builder</h2><p className="mt-1 text-sm text-muted-foreground">Trigger an email, assign an interviewer, and log activity when a candidate enters a stage.</p></div></div><div className="mt-5 flex flex-wrap items-center gap-2 text-sm"><span className="rounded-lg bg-muted px-3 py-2">Candidate enters Interview</span><span className="text-muted-foreground">→</span><span className="rounded-lg bg-muted px-3 py-2">Assign panel</span><span className="text-muted-foreground">→</span><span className="rounded-lg bg-muted px-3 py-2">Send prep email</span><span className="text-muted-foreground">→</span><span className="rounded-lg bg-muted px-3 py-2">Log activity</span></div></div></div>
}

function AnalyticsView({ onNotice }: { onNotice: (message: string) => void }) {
  const metrics = [['Time to hire', '18.6 days', '-12.8%'], ['Pipeline conversion', '24.8%', '+4.6%'], ['Offer acceptance', '86%', '+9.2%'], ['Candidate quality', '91/100', '+6.1%']]
  return <div><div className="mb-8 flex flex-col justify-between gap-5 md:flex-row md:items-end"><div><div className="mb-3 inline-flex items-center gap-2 rounded-full border border-border bg-card px-3 py-1.5 text-xs font-semibold text-primary"><BarChart3 className="size-3.5" />Phase 5 · Analytics</div><h1 className="text-3xl font-semibold tracking-tight md:text-4xl">Hiring performance, in focus.</h1><p className="mt-2 text-base text-muted-foreground">See where candidates move quickly and where the process can improve.</p></div><button onClick={() => onNotice('Analytics export prepared')} className="rounded-xl border border-border bg-card px-5 py-3 text-sm font-semibold">Export report</button></div><div className="grid gap-4 sm:grid-cols-2 xl:grid-cols-4">{metrics.map(([label, value, change]) => <div key={label} className="rounded-2xl border border-border bg-card p-5"><p className="text-sm text-muted-foreground">{label}</p><p className="mt-2 text-2xl font-semibold">{value}</p><span className="mt-2 inline-block rounded-full bg-emerald-50 px-2.5 py-1 text-xs font-semibold text-emerald-700">{change}</span></div>)}</div><div className="mt-6 grid gap-6 xl:grid-cols-[1.4fr_1fr]"><div className="rounded-2xl border border-border bg-card p-5"><div className="flex items-center justify-between"><div><h2 className="font-semibold">Pipeline velocity</h2><p className="mt-1 text-sm text-muted-foreground">Candidate movement over the last 30 days</p></div><span className="text-sm font-semibold text-primary">+18.4%</span></div><div className="mt-8 flex h-48 items-end gap-3">{[42, 58, 48, 75, 66, 88, 72, 94, 82, 100, 90, 108].map((height, index) => <div key={index} className="flex flex-1 flex-col justify-end gap-2"><div className="rounded-t-md bg-primary/80" style={{ height: `${height}px` }} /><span className="text-center text-[10px] text-muted-foreground">{['W1', '', 'W3', '', 'W5', '', 'W7', '', 'W9', '', 'W11', ''][index]}</span></div>)}</div></div><div className="rounded-2xl border border-border bg-card p-5"><h2 className="font-semibold">Top funnel drop-offs</h2><div className="mt-5 flex flex-col gap-4">{[['Resume review', '18%', 'bg-sky-500'], ['Interview loop', '11%', 'bg-violet-500'], ['Offer stage', '5%', 'bg-amber-500']].map(([label, value, color]) => <div key={label}><div className="mb-2 flex justify-between text-sm"><span>{label}</span><span className="font-semibold">{value}</span></div><div className="h-2 rounded-full bg-muted"><div className={`h-2 rounded-full ${color}`} style={{ width: value }} /></div></div>)}</div></div></div></div>
}

function IntegrationsView({ onNotice }: { onNotice: (message: string) => void }) {
  const connections = [{ title: 'Google Drive & Sheets', description: 'Sync job folders, candidate rows, and hiring reports.', icon: Cloud, accent: 'bg-sky-50 text-sky-600' }, { title: 'Email', description: 'Send candidate updates and interview prep automatically.', icon: Mail, accent: 'bg-amber-50 text-amber-600' }, { title: 'Slack', description: 'Share shortlist alerts and hiring activity with your team.', icon: MessageCircle, accent: 'bg-violet-50 text-violet-600' }, { title: 'Billing', description: 'Manage plan, seats, invoices, and usage limits.', icon: CreditCard, accent: 'bg-emerald-50 text-emerald-600' }, { title: 'White-labeling', description: 'Customize logo, accent color, and candidate-facing pages.', icon: Palette, accent: 'bg-rose-50 text-rose-600' }]
  return <div><div className="mb-8"><div className="mb-3 inline-flex items-center gap-2 rounded-full border border-border bg-card px-3 py-1.5 text-xs font-semibold text-primary"><PlugZap className="size-3.5" />Phase 6 · Connections</div><h1 className="text-3xl font-semibold tracking-tight md:text-4xl">Connect the rest of your stack.</h1><p className="mt-2 max-w-2xl text-base text-muted-foreground">Configure the tools your hiring team already uses. These cards are ready for OAuth and billing providers when credentials are connected.</p></div><div className="mb-6 rounded-2xl border border-primary/20 bg-primary/5 p-5"><div className="flex gap-3"><Sparkles className="mt-0.5 size-5 shrink-0 text-primary" /><div><h2 className="font-semibold">Phase 6 foundation ready</h2><p className="mt-1 text-sm leading-6 text-muted-foreground">The workspace surfaces are in place. Provider authentication, webhooks, billing checkout, and white-label domain provisioning should be wired one provider at a time.</p></div></div></div><div className="grid gap-4 md:grid-cols-2">{connections.map(({ title, description, icon: Icon, accent }) => <article key={title} className="rounded-2xl border border-border bg-card p-5"><div className="flex items-start justify-between gap-4"><div className={`flex size-11 items-center justify-center rounded-xl ${accent}`}><Icon className="size-5" /></div><span className="rounded-full bg-muted px-2.5 py-1 text-xs font-semibold text-muted-foreground">Not connected</span></div><h2 className="mt-5 font-semibold">{title}</h2><p className="mt-1 text-sm leading-6 text-muted-foreground">{description}</p><button onClick={() => onNotice(`${title} setup is ready for provider credentials`)} className="mt-5 w-full rounded-xl border border-border px-4 py-2.5 text-sm font-semibold hover:bg-muted">Configure connection</button></article>)}</div></div>
}

function IntakeView({ uploads, setUploads, onNotice }: { uploads: UploadItem[]; setUploads: Dispatch<SetStateAction<UploadItem[]>>; onNotice: (message: string) => void }) {
  function addFiles(files: FileList | null, kind: UploadItem['kind']) {
    if (!files?.length) return
    const next = Array.from(files).map((file, index) => ({ id: Date.now() + index, name: file.name, kind, size: `${Math.max(1, Math.round(file.size / 1024))} KB`, status: 'Processing' as const, candidate: kind === 'Resume' ? file.name.replace(/[-_].*/, '').replace(/\.[^.]+$/, '').replace(/\b\w/g, (m) => m.toUpperCase()) : undefined }))
    setUploads((current) => [...next, ...current])
    onNotice(`${next.length} ${kind.toLowerCase()} file${next.length > 1 ? 's' : ''} added`)
    window.setTimeout(() => setUploads((current) => current.map((item) => next.some((entry) => entry.id === item.id) ? { ...item, status: 'Uploaded' } : item)), 900)
  }
  return <div>
    <div className="mb-8 flex flex-col justify-between gap-5 md:flex-row md:items-end"><div><div className="mb-3 inline-flex items-center gap-2 rounded-full border border-border bg-card px-3 py-1.5 text-xs font-semibold text-primary"><FileText className="size-3.5" />Phase 2 · Intake</div><h1 className="text-3xl font-semibold tracking-tight md:text-4xl">Bring your hiring files together.</h1><p className="mt-2 max-w-2xl text-base text-muted-foreground">Upload job descriptions and resumes into your workspace. Local demo storage is active until a storage integration is connected.</p></div><span className="rounded-full bg-amber-50 px-3 py-2 text-xs font-semibold text-amber-700">Demo storage</span></div>
    <div className="grid gap-5 lg:grid-cols-2"><UploadCard title="Job descriptions" eyebrow="JD upload" description="Add the role context your matching workflow will use." accept=".pdf,.doc,.docx,.txt" icon={BriefcaseBusiness} onFiles={(files) => addFiles(files, 'JD')} /><UploadCard title="Candidate resumes" eyebrow="Resume upload" description="Drop PDF or DOCX resumes to create candidate records." accept=".pdf,.doc,.docx" icon={Users} onFiles={(files) => addFiles(files, 'Resume')} /></div>
    <div className="mt-6 overflow-hidden rounded-2xl border border-border bg-card"><div className="flex flex-col gap-2 border-b border-border p-5 sm:flex-row sm:items-center sm:justify-between"><div><h2 className="font-semibold">Candidate database</h2><p className="mt-1 text-sm text-muted-foreground">{uploads.filter((item) => item.kind === 'Resume').length} resume records in this workspace</p></div><button onClick={() => setUploads((current) => current.filter((item) => item.kind !== 'Resume'))} className="self-start rounded-lg border border-border px-3 py-2 text-xs font-semibold text-muted-foreground hover:text-foreground">Clear candidates</button></div><div className="divide-y divide-border">{uploads.filter((item) => item.kind === 'Resume').map((item) => <div key={item.id} className="flex flex-col gap-3 p-5 sm:flex-row sm:items-center sm:justify-between"><div className="flex items-center gap-3"><span className="flex size-10 items-center justify-center rounded-xl bg-violet-50 text-violet-600"><FileText className="size-5" /></span><div><p className="text-sm font-semibold">{item.candidate || item.name}</p><p className="text-xs text-muted-foreground">{item.name} · {item.size}</p></div></div><span className={`rounded-full px-2.5 py-1 text-xs font-semibold ${item.status === 'Uploaded' ? 'bg-emerald-50 text-emerald-700' : 'bg-amber-50 text-amber-700'}`}>{item.status}</span></div>)}{uploads.filter((item) => item.kind === 'Resume').length === 0 && <div className="p-10 text-center text-sm text-muted-foreground">Upload a resume to create the first candidate record.</div>}</div></div>
  </div>
}

function UploadCard({ title, eyebrow, description, accept, icon: Icon, onFiles }: { title: string; eyebrow: string; description: string; accept: string; icon: typeof FileText; onFiles: (files: FileList | null) => void }) {
  const inputId = title.replace(/\s/g, '-')
  return <div className="rounded-2xl border border-border bg-card p-5 shadow-[0_10px_30px_-24px_rgba(25,45,75,0.35)]"><div className="mb-5 flex items-start justify-between"><div><p className="mb-2 text-xs font-semibold uppercase tracking-[0.16em] text-primary">{eyebrow}</p><h2 className="text-lg font-semibold">{title}</h2><p className="mt-1 max-w-sm text-sm leading-6 text-muted-foreground">{description}</p></div><span className="flex size-10 items-center justify-center rounded-xl bg-sky-50 text-sky-600"><Icon className="size-5" /></span></div><label htmlFor={inputId} className="flex cursor-pointer flex-col items-center justify-center rounded-xl border border-dashed border-primary/30 bg-sky-50/40 px-5 py-8 text-center transition hover:border-primary hover:bg-sky-50"><span className="mb-3 flex size-10 items-center justify-center rounded-full bg-card text-primary shadow-sm"><Plus className="size-5" /></span><span className="text-sm font-semibold">Choose files to upload</span><span className="mt-1 text-xs text-muted-foreground">PDF, DOCX, or TXT · up to 10 MB</span><input id={inputId} type="file" multiple accept={accept} className="sr-only" onChange={(event) => onFiles(event.target.files)} /></label></div>
}

